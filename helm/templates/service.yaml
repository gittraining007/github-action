apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample-webapp.fullname" . }}
  labels:
    {{- include "sample-webapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sample-webapp.selectorLabels" . | nindent 4 }}
